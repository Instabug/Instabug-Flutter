import 'package:flutter/material.dart';

class APMScreen extends StatelessWidget {
  const APMScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'APM',
        ),
      ),
    );
  }
}
