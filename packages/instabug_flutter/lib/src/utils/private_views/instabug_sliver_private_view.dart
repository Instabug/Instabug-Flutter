import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class InstabugSliverPrivateView extends StatelessWidget {
  final Widget sliver;

  const InstabugSliverPrivateView({required this.sliver, Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return sliver;
  }
}
