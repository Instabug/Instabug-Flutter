import 'package:flutter/material.dart';

class InstabugPrivateView extends StatelessWidget {
  final Widget child;

  const InstabugPrivateView({required this.child, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
