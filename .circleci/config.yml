version: 2
jobs:
   build:
    docker:
      - image: cirrusci/flutter
    steps:
      - checkout
      - run: bundle check || sudo bundle install        
      - run: bundle exec danger
      - run: flutter doctor
      - run: pub get
      - run: flutter test
      - run: dartanalyzer --options analysis_options.yaml --fatal-warnings lib
      - run: flutter build
      - restore_cache:
          key: jars-{{ checksum "example/android/build.gradle" }}-{{ checksum  "example/android/app/build.gradle" }}
      - run:
          name: chmod permissions
          command: chmod +x ./example/android/gradlew
      - run:
          name: Generating Gradle wrapper files ⚙️...
          command: example/android/gradlew wrapper
      - run:
          name: Cleaning 🧹...
          command: example/android/gradlew clean
      - run:
          name: Building project 🏗...
          command: example/android/gradlew build    
      - run:
          name: Download Dependencies
          command: example/android/gradlew androidDependencies
      - save_cache:
          paths:
            - ~/example/android/.gradle
          key: jars-{{ checksum "example/android/build.gradle" }}-{{ checksum  "example/android/app/build.gradle" }}
      - run:
          name: Run UnitTest
          command: ./example/android/gradlew InstabugFlutterPluginTest
  #  android_test:
  #   working_directory: ~/project/example/android
  #   docker:
  #     # Android the primary container
  #     - image: circleci/android:api-28
  #   environment:
  #     JVM_OPTS: -Xmx3200m
  #   steps:
  #     - checkout:
  #         path: ~/project
  #     - restore_cache:
  #         key: jars-{{ checksum "build.gradle" }}-{{ checksum  "app/build.gradle" }}
  #     - run:
  #         name: chmod permissions
  #         command: chmod +x ./gradlew
  #     - run:
  #         name: Download Dependencies
  #         command: ./gradlew androidDependencies
  #     - save_cache:
  #         paths:
  #           - ~/.gradle
  #         key: jars-{{ checksum "build.gradle" }}-{{ checksum  "app/build.gradle" }}
  #     - run:
  #         name: Run UnitTest
  #         command: ./gradlew InstabugFlutterPluginTest
   release:
    docker:
      - image: cirrusci/flutter
    steps:
      - checkout
      - run: ./release.sh
      
workflows:
  version: 2
  build-test-and-approval-deploy:
    jobs:
      - build
      # - android_test
      - hold:
          type: approval
          requires:
            - build
            # - android_test
          filters:
            branches:
              only: master
      - release:
          requires:
            - hold
          filters:
            branches:
              only: master